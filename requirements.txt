# 台股動能飆股篩選器 — 相依套件
# 在 Juno / a-Shell / Pyto 上安裝：!pip install -r requirements.txt
# 或逐一安裝（見 README）

yfinance>=0.2.40      # 台股盤後股價（免費，免金鑰）
pandas>=2.0           # 資料處理
numpy>=1.24           # 數值運算
requests>=2.31        # 抓新聞 / 法人資料（證交所開放 API）

# 選用（可不裝，程式會自動降級）
matplotlib>=3.7       # 族群強度 / 輪動趨勢圖（--chart）；不裝則自動略過繪圖
# twstock>=1.3.1      # 台股代碼與即時報價輔助
